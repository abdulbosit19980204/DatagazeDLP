Some clients say "my computers are freezes by agent" but this programm doesn't freez. 
at this time run the "sturt up" and sort by using RAM, CPU. and stop them. 
DLP uses three service. They are "DgAgent","DgHostService" and "DgaMonitor".
If one of them stop, DLP doesn't work properly (correct)
